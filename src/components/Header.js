import React from 'react';
// The header has state, the text will change depending on the route from the router or on click


export default function Header() {
  return(
    <header class="header">Header</header>
  )
}